import { describe, it, expect } from 'vitest';

// Basic sanity check test. This ensures that the test runner is configured
// correctly and can execute tests. Real tests can be added later to
// exercise pages and components.

describe('basic setup', () => {
  it('works as expected', () => {
    expect(true).toBe(true);
  });
});
